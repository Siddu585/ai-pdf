import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Convert PDF to Word Online Free | PDF Utility Hub",
    description: "Instantly turn any PDF into an editable Microsoft Word document for free without losing formatting.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
